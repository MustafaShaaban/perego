# Corex Constitution — pointer

The canonical, authoritative constitution lives at:

**[`.specify/memory/constitution.md`](../.specify/memory/constitution.md)**

It is stored there because Spec Kit's `/speckit-constitution` workflow owns and regenerates
that file. This stub exists only so the documented source-of-truth hierarchy
(COREX-WORKING-GUIDE.md §A.1), which references `specs/constitution.md`, resolves to a real
file. There is exactly one source of truth — do not copy the rules here; edit the canonical
file via `/speckit-constitution`.
