/**
 * The documentation's current framework version. Kept in one place so the version
 * selector and any version-aware content read a single source. Bumped alongside the
 * framework release (the same value as COREX_CORE_VERSION / the plugin headers).
 */
export const CURRENT_VERSION = '0.35.1';

/** Where older releases and their notes live (real, published history). */
export const RELEASES_URL = 'https://github.com/MustafaShaaban/corex/releases';
