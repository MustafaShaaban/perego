{
    "name": "@corex/config",
    "version": "0.1.0",
    "private": true,
    "description": "Corex Config — admin screens (the Corex → Data DataViews app).",
    "license": "GPL-2.0-or-later",
    "scripts": {
        "build": "wp-scripts build --webpack-src-dir=src/admin --output-path=build/admin",
        "start": "wp-scripts start --webpack-src-dir=src/admin --output-path=build/admin"
    },
    "dependencies": {
        "@wordpress/components": "^36.1.0",
        "@wordpress/element": "^8.2.0",
        "@wordpress/i18n": "^6.24.0"
    }
}
