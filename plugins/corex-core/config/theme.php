<?php

/**
 * Theme defaults. `brand_path` is the per-site brand.json location (empty = the
 * active theme root). Overridable via the Config engine (`theme.brand_path`).
 *
 * @package Corex
 */

declare(strict_types=1);

defined('ABSPATH') || exit;

return [
    'brand_path' => '',
];
