# Model-routing test fixtures

The validator tests create a disposable copy of the committed routing assets, then introduce one isolated negative mutation. This fixture directory documents that strategy and keeps future malformed-agent examples scoped to model-routing tests.
