# Dependencies / build
/vendor/
/node_modules/
/**/build/
/**/dist/

# Environment / secrets (never commit)
.env
.env.*
wp-config.php

# Local AI / agent history + cache (private — NOT committed; the project memory is)
.corex/docs/
.corex/cache/
.corex/local-notes/
.ai/
.claude/local/
.codex/local/

# OS / editor
.DS_Store
Thumbs.db

# WordPress dev runtime — never committed, generated locally per DECISIONS.md
/wp/

# Raw design handoff reference (large binaries) — kept local, not shipped
/_design_handoff/
/Perego Creative Studio Final.zip
