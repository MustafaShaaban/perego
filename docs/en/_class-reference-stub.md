---
title: <ClassName>
description: One-line purpose of <ClassName>.
audience: contributor
stability: stable          # stable (class exists + documented) | planned (spec not implemented yet)
last_verified: null
---

<!--
  CLASS-REFERENCE LINK-STUB (spec 028, DECISIONS #50 + #62).

  Corex does NOT hand-write per-class reference pages — they would drift from the code.
  The authoritative, always-current class reference is GENERATED by `wp corex docs:generate`
  and published in docs-app. This stub exists only to (a) give the handbook a stable place to
  link a class by name, and (b) point at the generated page. Keep it to a few lines.

  If the class is not built yet, set stability: planned and link to the Spec Kit module that
  will produce it (specs/NNN-…) instead of a docs-app page. Never invent methods or signatures.
-->

`<Corex\…\ClassName>` — <one sentence on what it does and which layer it belongs to>.

- **Full reference (generated):** [docs-app → reference/<layer>/<classname>](../../../docs-app/src/content/docs/reference/<layer>/<classname>.md)
  <!-- On the published site this is /reference/<layer>/<classname>/. Regenerate with `wp corex docs:generate`. -->
- **Defined in:** `<path/to/Class.php>`
- **Used in a task?** See the relevant cookbook: [../06-cookbooks/<scenario>.md](../06-cookbooks/)

<!-- For a PLANNED class instead: -->
<!-- > **stability: planned** — this class is specified but not yet implemented. See
     [specs/NNN-module](../../../specs/NNN-module/spec.md). The reference page appears once it ships. -->
